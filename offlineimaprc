[general]
ui = ttyui
accounts = Personal,Better
pythonfile = ~/Documents/Projects/dotfiles/offlineimaprc.py
maxsyncaccounts = 5

[Account Personal]
localrepository = Personal-Local
remoterepository = Personal-Remote
status_backend = sqlite
autorefresh = 5
quick = 10

[Account Better]
localrepository = Better-Local
remoterepository = Better-Remote
status_backend = sqlite
autorefresh = 5
quick = 10

[Repository Personal-Local]
type = Maildir
localfolders = ~/Mail/Personal

[Repository Better-Local]
type = Maildir
localfolders = ~/Mail/Better

[Repository Personal-Remote]
type = Gmail
remoteuser = jaspervdj@gmail.com
remotepasseval = get_gmail_password('jaspervdj@gmail.com')
realdelete = no
sslcacertfile = /usr/local/opt/curl-ca-bundle/share/ca-bundle.crt
folderfilter = sync_folder

[Repository Better-Remote]
type = Gmail
remoteuser = jasper.van.der.jeugt@better.com
remotepasseval = get_gmail_password('jasper.van.der.jeugt@better.com')
realdelete = no
sslcacertfile = /usr/local/opt/curl-ca-bundle/share/ca-bundle.crt
folderfilter = sync_folder
